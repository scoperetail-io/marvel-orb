description: >
  This command write common files to the project.

steps:
  - run:
      name: Writting file DockerFile
      command: <<include(scripts/writer-dockerfile.sh)>>
  - run:
      name: Writting file layers.xml
      command: <<include(scripts/writer-layers-xml.sh)>>
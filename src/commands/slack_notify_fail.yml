description: "Slack Notify Success"
steps:
  - slack/notify:
      event: always
      custom: |
        {
        	"blocks": [
        		{
        			"type": "section",
        			"text": {
        				"type": "mrkdwn",
        				"text": "*Failed* :red_circle: | <${CIRCLE_REPOSITORY_URL}|${CIRCLE_PROJECT_REPONAME}> | <${CIRCLE_BUILD_URL}|${CIRCLE_JOB}> | ${CIRCLE_USERNAME}"
        			}
        		}
        	]
        }

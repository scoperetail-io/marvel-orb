description: |
  Checkout, build, test, and upload test results for a Maven project.
executor: <<parameters.executor>>

parameters:
  executor:
    description: The name of custom executor to use
    type: executor
    default: java11

steps:
  - checkout
  - with_cache:
      steps:
        - run:
            name: Build and Deploy
            command: |
              mvn -s .circleci/.maven.xml clean deploy
        - slack_notify_fail

